name: CI
on:
  pull_request:
    branches: [ master, dev, main ]

permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  CI:
    uses: c12qe/github_actions/.github/workflows/ci.yml@master
    secrets: inherit
    with:
      run_audit: true